# Ensemble workflow

Plan the task, implement the smallest complete change, review it against the
task's acceptance criteria, and document user-visible behavior when necessary.

A run is complete only after relevant automated tests pass. Runtime results must
be structured and must select a valid next role for every non-terminal outcome.
