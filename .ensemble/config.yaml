runtime:
  name: codex
  config:
    sandbox: workspace-write
initialRole: planner
terminalOutcomes: [approved, completed]
statuses:
  runnable: [ready, running]
  running: running
  completed: completed
  failed: failed
  blocked: blocked
